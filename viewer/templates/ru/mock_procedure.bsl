
 	ПараметрыМетода = Мокито.МассивПараметров({2});

 	ПрерватьВыполнение = Ложь;
 	Мокито.АнализВызова({0}, "{1}", ПараметрыМетода, ПрерватьВыполнение);

 	Если НЕ ПрерватьВыполнение Тогда
 		ПродолжитьВызов({2});
 	КонецЕсли;
