
 	ПараметрыМетода = Мокито.МассивПараметров({2});

 	ПрерватьВыполнение = Ложь;
 	Результат = Мокито.АнализВызова({0}, "{1}", ПараметрыМетода, ПрерватьВыполнение);

 	Если НЕ ПрерватьВыполнение Тогда
 		Возврат ПродолжитьВызов({2});
 	Иначе
 		Возврат Результат;
 	КонецЕсли;
